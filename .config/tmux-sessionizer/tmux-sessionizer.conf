# tmux-sessionizer configuration
TS_SEARCH_PATHS=(~/automata ~/go /Users/tom ~/.config ~/tallpress)

# Session commands - use with -s flag (e.g., tmux-sessionizer -s 0)
TS_SESSION_COMMANDS=("nvim ." "claude")
